#include <stdio.h>
#include <string.h>

const int MAX_SIZE = 5;

int main() {
  char *history[MAX_SIZE];
  char *input = NULL;
  size_t len = 0;

  while (1) {
    printf("Enter input: ");
    len = getline(&input, &len, stdin);
    input[len - 1] = '\0';

    if (strcmp(input, "print") == 0) {
      printf("Test input");
    }
  }

  return 0;
}
